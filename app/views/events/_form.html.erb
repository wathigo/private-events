<%= form_for @event do |f| %>
  <%= f.label :description %>
  <%= f.text_area :description, size: "10x10", class: "form-control" %>

  <%= f.submit yield(:button), class: "btn btn-primary" %>
<% end %>
